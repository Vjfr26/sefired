# Solo para `npm run dev` (Vite modo development) — el build de producción
# (npm run build, p.ej. para BanaHosting) sigue usando el .env normal.
# Vacío = rutas relativas /api/* resueltas por el proxy de Vite hacia el
# backend local (localhost:8000, ver vite.config.js).
VITE_API_URL=
