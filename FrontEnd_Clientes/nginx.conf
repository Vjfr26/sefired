server {
    listen 5174;
    root /usr/share/nginx/html;
    index index.html;

    # DNS embebido de Docker — sin esto, nginx resuelve "backend" una sola
    # vez al iniciar y se queda con esa IP para siempre. Si el contenedor
    # backend se recrea (ej. al desplegar solo el backend sin reiniciar este
    # frontend), la IP cambia y nginx sigue apuntando a la vieja → 502
    # "Connection refused" hasta reiniciar este contenedor manualmente.
    resolver 127.0.0.11 valid=10s;

    # Compresión — reduce el payload hasta 70%
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types
        text/html text/css text/javascript
        application/javascript application/json
        image/svg+xml font/woff2;

    # Assets con hash (JS/CSS generados por Vite) — caché de 1 año
    location ~* /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Imágenes, fuentes, iconos — caché de 30 días
    location ~* \.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public";
        access_log off;
    }

    # js/main.js no tiene hash de contenido en el nombre (no lo procesa Vite,
    # se referencia como <script src> clásico) — sin esto el navegador lo
    # cachea de forma heurística y puede seguir sirviendo una versión vieja
    # después de cada despliegue.
    location = /js/main.js {
        add_header Cache-Control "no-cache, must-revalidate";
        expires 0;
    }

    # SPA — cualquier ruta que no sea archivo devuelve index.html
    location / {
        try_files $uri $uri/ /index.html;

        # index.html NO se cachea (el browser necesita la versión más reciente)
        location = /index.html {
            add_header Cache-Control "no-cache, no-store, must-revalidate";
            expires 0;
        }
    }

    # Proxy hacia el backend para /api y /storage.
    # "set $backend_upstream" + variable en proxy_pass (en vez del nombre de
    # host literal) es lo que obliga a nginx a re-resolver con el "resolver"
    # de arriba en cada request, en vez de solo una vez al arrancar.
    location /api/ {
        set $backend_upstream http://backend:8000;
        proxy_pass $backend_upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 60s;
    }

    location /storage/ {
        set $backend_upstream http://backend:8000;
        proxy_pass $backend_upstream;
        proxy_set_header Host $host;
    }
}
